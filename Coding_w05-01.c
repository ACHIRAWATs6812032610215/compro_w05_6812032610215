#include <stdio.h>

int main() {
    char name = 'Toh';
    int age = 19;
    float weight = 80;

    printf("Student %c is %d years old.\n", name, age);
    printf("His weighs is %.1f kg.\n", weight);

    return 0;
}